(()=>{const track=document.getElementById('workTrack');const prev=document.getElementById('workPrev');const next=document.getElementById('workNext');const progress=document.getElementById('workProgress');function step(){const card=track.querySelector('.project');return card?card.getBoundingClientRect().width+18:600}function move(dir){track.scrollBy({left:step()*dir,behavior:'smooth'})}prev.addEventListener('click',()=>move(-1));next.addEventListener('click',()=>move(1));track.addEventListener('keydown',e=>{if(e.key==='ArrowRight'){e.preventDefault();move(1)}if(e.key==='ArrowLeft'){e.preventDefault();move(-1)}});track.addEventListener('scroll',()=>{const max=track.scrollWidth-track.clientWidth;const ratio=max>0?track.scrollLeft/max:0;progress.style.width=`${25+ratio*75}%`},{passive:true});const image=document.getElementById('serviceImage');const services=[...document.querySelectorAll('.service')];function activate(button){if(button.classList.contains('active'))return;services.forEach(x=>x.classList.remove('active'));button.classList.add('active');image.style.opacity='0';setTimeout(()=>{image.src=button.dataset.img;image.style.opacity='1'},160)}services.forEach(button=>{button.addEventListener('mouseenter',()=>activate(button));button.addEventListener('focus',()=>activate(button));button.addEventListener('click',()=>activate(button))});const reveal=[...document.querySelectorAll('.intro,.section-heading,.project,.services-copy,.services-media,.process-row,.contact-copy,.contact-media')];if('IntersectionObserver'in window&&!matchMedia('(prefers-reduced-motion: reduce)').matches){reveal.forEach(el=>el.classList.add('reveal'));const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.12});reveal.forEach(el=>observer.observe(el));}})();