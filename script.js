(()=>{const track=document.getElementById('workTrack');const prev=document.getElementById('workPrev');const next=document.getElementById('workNext');const progress=document.getElementById('workProgress');if(track&&prev&&next){const step=()=>{const card=track.querySelector('.project');return card?card.getBoundingClientRect().width+18:640};const move=dir=>track.scrollBy({left:step()*dir,behavior:'smooth'});prev.addEventListener('click',()=>move(-1));next.addEventListener('click',()=>move(1));track.addEventListener('keydown',e=>{if(e.key==='ArrowRight'){e.preventDefault();move(1)}if(e.key==='ArrowLeft'){e.preventDefault();move(-1)}});const updateProgress=()=>{if(!progress)return;const max=Math.max(1,track.scrollWidth-track.clientWidth);const ratio=Math.min(1,Math.max(0,track.scrollLeft/max));progress.style.width=`${25+ratio*75}%`};track.addEventListener('scroll',updateProgress,{passive:true});window.addEventListener('resize',updateProgress);updateProgress()}const image=document.getElementById('serviceImage');const services=[...document.querySelectorAll('.service')];if(image&&services.length){const activate=button=>{services.forEach(x=>x.classList.remove('active'));button.classList.add('active');const nextSrc=button.dataset.img;if(!nextSrc||image.src===nextSrc)return;image.style.opacity='.25';setTimeout(()=>{image.src=nextSrc;image.style.opacity='1'},120)};services.forEach(button=>{button.addEventListener('mouseenter',()=>activate(button));button.addEventListener('focus',()=>activate(button));button.addEventListener('click',()=>activate(button))})}})();
